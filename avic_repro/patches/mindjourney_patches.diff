diff -ru -x .git -x wan2.2 -x assets -x __pycache__ -x test /tmp/claude-1004/-home-haoming-reason-branch-0824/c3e470d7-4bf1-43e9-9c6e-e451ef1d68f9/scratchpad/mindjourney/stable_virtual_camera/seva/modules/autoencoder.py mindjourney/stable_virtual_camera/seva/modules/autoencoder.py
--- /tmp/claude-1004/-home-haoming-reason-branch-0824/c3e470d7-4bf1-43e9-9c6e-e451ef1d68f9/scratchpad/mindjourney/stable_virtual_camera/seva/modules/autoencoder.py	2026-08-27 08:19:41.831558930 -0400
+++ mindjourney/stable_virtual_camera/seva/modules/autoencoder.py	2026-08-27 08:27:33.581697219 -0400
@@ -1,51 +1,51 @@
-import torch
-from diffusers.models import AutoencoderKL  # type: ignore
-from torch import nn
-
-
-class AutoEncoder(nn.Module):
-    scale_factor: float = 0.18215
-    downsample: int = 8
-
-    def __init__(self, chunk_size: int | None = None):
-        super().__init__()
-        self.module = AutoencoderKL.from_pretrained(
-            "stabilityai/stable-diffusion-2-1-base",
-            subfolder="vae",
-            force_download=False,
-            low_cpu_mem_usage=False,
-        )
-        self.module.eval().requires_grad_(False)  # type: ignore
-        self.chunk_size = chunk_size
-
-    def _encode(self, x: torch.Tensor) -> torch.Tensor:
-        return (
-            self.module.encode(x).latent_dist.mean  # type: ignore
-            * self.scale_factor
-        )
-
-    def encode(self, x: torch.Tensor, chunk_size: int | None = None) -> torch.Tensor:
-        chunk_size = chunk_size or self.chunk_size
-        if chunk_size is not None:
-            return torch.cat(
-                [self._encode(x_chunk) for x_chunk in x.split(chunk_size)],
-                dim=0,
-            )
-        else:
-            return self._encode(x)
-
-    def _decode(self, z: torch.Tensor) -> torch.Tensor:
-        return self.module.decode(z / self.scale_factor).sample  # type: ignore
-
-    def decode(self, z: torch.Tensor, chunk_size: int | None = None) -> torch.Tensor:
-        chunk_size = chunk_size or self.chunk_size
-        if chunk_size is not None:
-            return torch.cat(
-                [self._decode(z_chunk) for z_chunk in z.split(chunk_size)],
-                dim=0,
-            )
-        else:
-            return self._decode(z)
-
-    def forward(self, x: torch.Tensor) -> torch.Tensor:
-        return self.decode(self.encode(x))
+import torch
+from diffusers.models import AutoencoderKL  # type: ignore
+from torch import nn
+
+
+class AutoEncoder(nn.Module):
+    scale_factor: float = 0.18215
+    downsample: int = 8
+
+    def __init__(self, chunk_size: int | None = None):
+        super().__init__()
+        self.module = AutoencoderKL.from_pretrained(
+            __import__("os").environ.get("SVC_VAE_PATH", "/home/haoming/.cache/huggingface/hub/models--friedrichor--stable-diffusion-2-1-realistic/snapshots/62cc582044fd762a1e2db3ee7c24309f1646f8c4"),  # REPRO PATCH
+            subfolder="vae",
+            force_download=False,
+            low_cpu_mem_usage=False,
+        )
+        self.module.eval().requires_grad_(False)  # type: ignore
+        self.chunk_size = chunk_size
+
+    def _encode(self, x: torch.Tensor) -> torch.Tensor:
+        return (
+            self.module.encode(x).latent_dist.mean  # type: ignore
+            * self.scale_factor
+        )
+
+    def encode(self, x: torch.Tensor, chunk_size: int | None = None) -> torch.Tensor:
+        chunk_size = chunk_size or self.chunk_size
+        if chunk_size is not None:
+            return torch.cat(
+                [self._encode(x_chunk) for x_chunk in x.split(chunk_size)],
+                dim=0,
+            )
+        else:
+            return self._encode(x)
+
+    def _decode(self, z: torch.Tensor) -> torch.Tensor:
+        return self.module.decode(z / self.scale_factor).sample  # type: ignore
+
+    def decode(self, z: torch.Tensor, chunk_size: int | None = None) -> torch.Tensor:
+        chunk_size = chunk_size or self.chunk_size
+        if chunk_size is not None:
+            return torch.cat(
+                [self._decode(z_chunk) for z_chunk in z.split(chunk_size)],
+                dim=0,
+            )
+        else:
+            return self._decode(z)
+
+    def forward(self, x: torch.Tensor) -> torch.Tensor:
+        return self.decode(self.encode(x))
diff -ru -x .git -x wan2.2 -x assets -x __pycache__ -x test /tmp/claude-1004/-home-haoming-reason-branch-0824/c3e470d7-4bf1-43e9-9c6e-e451ef1d68f9/scratchpad/mindjourney/utils/api.py mindjourney/utils/api.py
--- /tmp/claude-1004/-home-haoming-reason-branch-0824/c3e470d7-4bf1-43e9-9c6e-e451ef1d68f9/scratchpad/mindjourney/utils/api.py	2026-08-27 08:19:41.832558939 -0400
+++ mindjourney/utils/api.py	2026-08-27 08:27:33.580697210 -0400
@@ -1,4 +1,5 @@
 from openai import AzureOpenAI
+from openai import OpenAI
 from typing import Dict, List
 import logging
 import base64
@@ -32,11 +33,20 @@
         self.messages: List[Dict[str, str]] = []
         self.history: List[Dict[str, str]] = []
 
-        self.client = AzureOpenAI(
-            api_key=config.api_key,
-            api_version=config.api_version,
-            azure_endpoint=config.azure_endpoint,
-        )
+        # --- REPRO PATCH: plain OpenAI backend when OPENAI_API_KEY is set and no Azure endpoint.
+        azure_ep = os.environ.get("AZURE_OPENAI_ENDPOINT", "")
+        openai_key = os.environ.get("OPENAI_API_KEY", "")
+        if openai_key and not azure_ep:
+            self.client = OpenAI(api_key=openai_key, max_retries=5, timeout=120)
+            self.backend = "openai"
+        else:
+            self.client = AzureOpenAI(
+                api_key=config.api_key,
+                api_version=config.api_version,
+                azure_endpoint=config.azure_endpoint,
+            )
+            self.backend = "azure"
+        print(f"[ChatAPI] backend={self.backend} model={config.model}")
 
         self.model = config.model
         self.max_limit = min(config.limit - 2000, 20000)
@@ -71,7 +81,6 @@
         self.history.append(self.messages[-1])
 
     def get_system_response(self) -> str:
-        return "understanding:\n\nAction: turn-right 40"
         try:
             self.do_truncation = False
 
diff -ru -x .git -x wan2.2 -x assets -x __pycache__ -x test /tmp/claude-1004/-home-haoming-reason-branch-0824/c3e470d7-4bf1-43e9-9c6e-e451ef1d68f9/scratchpad/mindjourney/utils/vlm_wrapper.py mindjourney/utils/vlm_wrapper.py
--- /tmp/claude-1004/-home-haoming-reason-branch-0824/c3e470d7-4bf1-43e9-9c6e-e451ef1d68f9/scratchpad/mindjourney/utils/vlm_wrapper.py	2026-08-27 08:19:41.832558939 -0400
+++ mindjourney/utils/vlm_wrapper.py	2026-08-27 08:27:33.580697210 -0400
@@ -35,8 +35,7 @@
         elif model_name in ['OpenGVLab/InternVL3-8B', 'OpenGVLab/InternVL3-14B']:
             assert qa_model_name in (None, "None") or qa_model_name == model_name, "Separate Score/QA model is not supported for InternVL3."
             # device_map = split_model(model_name)
-            device_map = "cuda:1"
-            device_map = split_model(model_name)
+            device_map = "cuda:0" if torch.cuda.device_count() == 1 else split_model(model_name)  # REPRO PATCH
             self.model = AutoModel.from_pretrained(
                 model_name,
                 torch_dtype=torch.bfloat16,
@@ -88,7 +87,7 @@
                 response = self.model.get_system_response_with_content(system_prompt, content)
             return response
         elif self.prompt_style == 'internvl3':
-            content = format_internvl3_content(content, "cuda:1")
+            content = format_internvl3_content(content, "cuda:0")
             # content = format_internvl3_content(content, None)
             # content = format_internvl3_content(content)
             question = content['question']
